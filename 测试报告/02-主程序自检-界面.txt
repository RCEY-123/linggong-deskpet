灵工桌宠 · 自检报告
时间：2026-09-20 16:55:12
版本：1.1.0  作者：睡不着のHATSUZUKI
安装目录：<工程根>\dist\app
系统：Microsoft Windows NT 6.2.9200.0 / .NET 4.0.30319.42000
模式：ui（core=功能核心 / ui=界面与交互 / all=全部）
临时目录：%TEMP%\aldeskpet-selftest-20260920-165512-35972-877467

== ⑮ 界面健壮性（气泡复用 / 弹层避让 / 自适应布局） ==
  [PASS] 气泡连续 3 次显示/自动收起后可继续复用  → 无异常、未被销毁
  [PASS] 语音未播完时气泡不会被自动收起（需求 4）  → 仍显示
  [PASS] 语音播完后气泡按设定时间收起  → 已收起
  [PASS] 手动收起后实例仍可用
  [PASS] 气泡不与立绘重叠  → {X=1496,Y=518,Width=420,Height=180}
  [PASS] 气泡在工作区内  → {X=1496,Y=518,Width=420,Height=180}
  [PASS] 输入框不与立绘重叠（需求 3）  → {X=1168,Y=966,Width=420,Height=54}
  [PASS] 输入框在工作区内  → {X=1168,Y=966,Width=420,Height=54}
  [PASS] 输入框落在角色下方或侧边  → {X=1168,Y=966,Width=420,Height=54}
  [PASS] 立绘贴屏幕顶部时气泡也不压住角色  → {X=1068,Y=146,Width=420,Height=180}
  [PASS] 放大后页签面板跟着变大  → {Width=526, Height=550} → {Width=1266, Height=870}
  [PASS] 放大后所有顶层控件仍在客户区内
  [PASS] 放大后内容没有挤在左上角（左下角色面板变高）  → 914 px
  [PASS] 全屏尺寸截图  → 122.5 KB

== ⑱ 界面细节回归（对勾 / 标题重复 / 角色卡两列 / 右键菜单） ==
  [PASS] 输入条「发送」按钮不画对勾（需求 1）  → ShowCheck=False
  [PASS] 输入条「发送」按钮仍然可见可用
  [PASS] 模式按钮开启对勾显示
  [PASS] 放大后标题栏中间没有残留的第二份右上角标识（需求 2）  → 中间区域金色像素 = 0
  [PASS] 右上角的宋体字仍在  → 右侧金色像素 = 163
  [PASS] 角色卡左右两列不重叠（1000×700）  → persona={X=14,Y=130,Width=345,Height=194} style={X=375,Y=130,Width=317,Height=84} greeting={X=375,Y=244,Width=317,Height=25} rules={X=375,Y=300,Width=317,Height=60}
  [PASS] 右列整体位于左列右侧（1000×700）  → persona.Right=359 style.Left=375
  [PASS] 右列在窗口内有足够宽度（1000×700）  → style.W=317 rules.W=317
  [PASS] 角色卡左右两列不重叠（1160×780）  → persona={X=14,Y=130,Width=428,Height=274} style={X=458,Y=130,Width=394,Height=84} greeting={X=458,Y=244,Width=394,Height=25} rules={X=458,Y=300,Width=394,Height=104}
  [PASS] 右列整体位于左列右侧（1160×780）  → persona.Right=442 style.Left=458
  [PASS] 右列在窗口内有足够宽度（1160×780）  → style.W=394 rules.W=394
  [PASS] 角色卡左右两列不重叠（1900×1100）  → persona={X=14,Y=130,Width=813,Height=594} style={X=843,Y=130,Width=749,Height=84} greeting={X=843,Y=244,Width=749,Height=25} rules={X=843,Y=300,Width=749,Height=424}
  [PASS] 右列整体位于左列右侧（1900×1100）  → persona.Right=827 style.Left=843
  [PASS] 右列在窗口内有足够宽度（1900×1100）  → style.W=749 rules.W=749
  [PASS] 右键菜单有「关闭桌宠并打开设置界面」（需求 4）
  [PASS] 右键菜单仍保留「打开设置界面」
  [PASS] 右键菜单的退出项写明是退出程序

== ⑰ 对话模式切换（回归：切不动 / 两个按钮同时亮） ==
  [PASS] 互动设置页可见  → InteractTab
  [PASS] 找到两个模式按钮
  [PASS] 点「固定台词模式」后模式确实变成 fixed  → fixed
  [PASS] 固定台词按钮为选中态、大模型按钮不是  → fixed.Primary=True llm.Primary=False
  [PASS] 点「大模型对话模式」后模式变成 llm  → llm
  [PASS] 大模型按钮为选中态、固定台词按钮不是  → fixed.Primary=False llm.Primary=True
  [PASS] 选中「大模型」时它带金色对勾
  [PASS] 未选中的「固定台词」没有金色对勾（不会看着也像选中）
  [PASS] 切回「固定台词」后对勾跟着换过去
  [PASS] 连续切换 6 次状态始终互斥
  [PASS] 最终模式与按钮一致  → fixed

== ⑯ 桌宠互动回归（大模型模式点击 / 主动对话 / 语音节流） ==
  [PASS] 大模型模式下点击桌宠不抛异常
  [PASS] 模型回复出现在气泡里（不是从对话框里回）  → 气泡内容：指挥官…妾身一直在等汝。
  [PASS] 气泡可见
  [PASS] 连续快速点击不报错（回归：气泡被销毁）
  [PASS] 多次点击后气泡仍能显示模型回复  → 气泡内容：指挥官…妾身一直在等汝。
  [PASS] 主动对话会弹出气泡（回归：只响不出话）  → 气泡可见=True 内容：指挥官…妾身一直在等汝。
  [PASS] 固定台词模式能出词  → 指挥官…妾身一直在等汝。
  [PASS] 语音未播完时互动不切换台词（需求 4）  → 「指挥官…妾身一直在等汝。」→「指挥官…妾身一直在等汝。」
  [PASS] 语音结束后互动恢复正常出词  → 指挥官…妾身一直在等汝。

== ⑳ 桌宠显示：真实窗口行为与设置页 ==
  [PASS] 设置界面新增「桌宠显示」页  → sound、llm、path、log、general、display、about
  [PASS] 设置页数量与页签数量一致  → 7 个页签
  [PASS] 桌宠显示页有内容  → 2 个控件
  [PASS] 三种显示方式按钮齐全且带选中对勾
  [PASS] 按钮文案就是三种显示方式  → 始终保持在最上层 / 其他应用获得焦点时冻结 / 其他应用全屏时隐藏
  [PASS] 点「其他应用获得焦点时冻结」立即写入配置  → focus
  [PASS] 当前选中的那一项打上金色对勾（其余不打）
  [PASS] 状态文字跟着更新  → 当前：其他应用获得焦点时冻结；有其他程序全屏时自动隐藏
  [PASS] 点「其他应用全屏时隐藏」也生效  → 当前：其他应用全屏时隐藏
  [PASS] 配置里出现未知值也能收敛回「始终最上层」  → always
  [PASS] 桌宠能在「始终最上层」模式下启动
  [PASS] 能按角色 id 取到桌面上的桌宠窗口  → 已取到
  [PASS] 缺省状态：正常显示且是最上层窗口
  [PASS] 「始终最上层 + 不隐藏全屏」时不起显示监听（零轮询）  → Running=False
  [PASS] 冻结之前气泡是显示出来的
  [PASS] 冻结：窗口还在桌面上（不是直接藏起来）
  [PASS] 冻结：不再是置顶窗口
  [PASS] 冻结：动画与自动搭话已暂停  → DisplayState=Sunk
  [PASS] 冻结：气泡被收起（不会浮在别人程序上面）
  [PASS] 冻结期间不会主动开口（需求：别打扰正在用别的软件的人）  → 气泡内容：冻结之前先开着的泡泡
  [PASS] 冻结后沉到别的窗口下面（不会挡住其他软件）  → 桌宠 Z=623，别的窗口 Z=66
  [PASS] 沉底后仍在桌面之上（不会被壁纸/图标盖住）  → 桌宠 Z=623，桌面 Z=625
  [PASS] 回到桌面：立刻恢复置顶
  [PASS] 回到桌面：动画与搭话重新开始
  [PASS] 回到桌面：窗口可见且画布正常
  [PASS] 全屏时：窗口隐藏（进后台运行）
  [PASS] 全屏时：仍然保持置顶标志（回来时直接在最上面）
  [PASS] 全屏时：动画与搭话也是暂停的
  [PASS] 后台状态下不会冒出问候气泡（开机自启正赶上全屏程序时同理）  → 气泡内容：冻结之前先开着的泡泡
  [PASS] 回到桌面：重新显示出来
  [PASS] 回到桌面：分层窗口画面正常（贴图没丢）
  [PASS] 重复应用显示状态不抛异常（幂等）
  [PASS] 打开「全屏自动隐藏」后显示监听自动起来
  [PASS] 监听在按间隔采样  → 900ms 内采样 1 次
  [PASS] 桌宠的实际状态与「当前前台窗口」的判定一致  → 状态=正常显示，判定=正常显示，前台=桌宠自己
  [PASS] 前台窗口采样结果可用  → 桌宠自己
  [PASS] 关掉全部显示策略后监听立刻停（回到零轮询）
  [PASS] 关掉后桌宠回到最上层
  [PASS] 桌宠右键菜单里有「桌宠显示」
  [PASS] 菜单里三种显示方式齐全  → 3 项
  [PASS] 菜单里只有当前这一种被标成 ●  → 1 项被标记：● 始终保持在最上层
  [PASS] 菜单里的全屏开关与配置一致  → ○ 有程序全屏时自动隐藏（进后台）
  [PASS] 菜单里能直接开关全屏自动隐藏  → 现在是 True
  [PASS] 关掉桌宠后显示监听一并停止
  [PASS] 关掉后桌宠不再显示在桌面上

== ⑭ 界面离屏渲染（视觉检查） ==
  [PASS] 主界面渲染：main-1.png  → 395.9 KB
  [PASS] 主界面渲染：main-2.png  → 122.7 KB
  [PASS] 主界面渲染：main-3.png  → 108.1 KB
  [PASS] 主界面渲染：main-4.png  → 122.6 KB
  [PASS] 主界面渲染：main-5.png  → 105.3 KB
  [PASS] 主界面渲染：main-6.png  → 96.1 KB
  [PASS] 设置界面渲染：settings-sound.png  → 33.2 KB
  [PASS] 设置界面渲染：settings-llm.png  → 44.6 KB
  [PASS] 设置界面渲染：settings-path.png  → 41.2 KB
  [PASS] 设置界面渲染：settings-log.png  → 69.6 KB
  [PASS] 设置界面渲染：settings-general.png  → 37.3 KB
  [PASS] 设置界面渲染：settings-display.png  → 62.6 KB
  [PASS] 设置界面渲染：settings-about.png  → 48 KB
  [PASS] 桌宠画布已生成  → 357×372
  [PASS] 桌宠窗口渲染：pet-sprite.png  → 357×372
  [PASS] 桌宠立绘可见像素占比正常  → 58.0%
  [PASS] Q 弹动画可以采样到中间帧  → 已采样
  [PASS] 桌宠问候气泡已弹出  → 见 bubble.png
  [PASS] 气泡画布已生成  → 414×185
  [PASS] 气泡渲染：bubble.png  → 414×185
  [PASS] 气泡暗色背景可读性样例：bubble-on-dark.png
  · 截图目录：<工程根>\dist\ui-shots

== ㉒ 关机 / 注销不阻止关机（回归：桌宠在跑时 Windows 报「正在阻止关机」） ==
  [PASS] 关机请求 → 立刻放行（不取消、不弹确认框）
  [PASS] 任务管理器结束任务 → 立刻放行
  [PASS] Application.Exit → 立刻放行
  [PASS] 已在退出流程中 → 直接放行
  [PASS] 用户点 X 且桌宠在跑 → 只收起界面（不退出）
  [PASS] 用户点 X 且桌宠没跑 → 问一句再退
  [PASS] 普通关闭原因不算系统关机
  [PASS] 测试用桌宠已启动
  [PASS] 桌宠在跑时点 X 只收起界面（不退出、不弹退出确认）
  [PASS] 收起界面后桌宠仍在桌面上
  [PASS] 关机请求不被取消（这就是「阻止关机」的根因）
  [PASS] 关机时会先把桌宠全部停掉
  [PASS] 关机时标记 ShuttingDown（后续关闭一律放行）
  [PASS] 关机时抑制一切弹窗（不会卡在 MessageBox 上）
  [PASS] 关机时桌宠显示监听也停了

================ 结果 ================
通过 132 项，失败 0 项，用时 29.9 秒
结论：全部通过 ✔
